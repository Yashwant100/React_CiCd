name: "Sample React Ci Pipeline"
on:
  push:
    branches: 
      -["master"]
       ["main"]

jobs:
  Build-And-Test:
    runs-on: ubuntu-latest
    steps:
      - name: "Setup Node Js"
        uses: actions/setup-node@v4
        with:
          node-version: "16"



